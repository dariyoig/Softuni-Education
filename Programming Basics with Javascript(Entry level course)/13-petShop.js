function calculateFoodCost([dogBags, catBags]) {
    let dogFoodCost = dogBags * 2.50;
    let catFoodCost = catBags * 4;

    console.log(`${dogFoodCost+ catFoodCost} lv.`);
}
calculateFoodCost(['4', '6']);
