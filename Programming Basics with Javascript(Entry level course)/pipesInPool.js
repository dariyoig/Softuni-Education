function solve([arg1, arg2, arg3, arg4]) {
    let poolVolume = Number(arg1);
    let firstPipeDebit = Number(arg2);
    let secondPipeDebit = Number(arg3);
    let hoursGone = Number(arg4);

    
    console.log(`The pool is {запълненост на басейна в проценти}% full. Pipe 1: {процент вода от първата тръба}%. Pipe 2: {процент вода от втората тръба}%.`);
    console.log(`For {часовете, които тръбите са пълнили вода} hours the pool overflows with {литрите вода в повече} liters.`);


}
solve([1000,
    100,
    120,
    3])